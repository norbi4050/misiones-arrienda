/**
 * Supabase Auth Configuration Constants
 * 
 * Centralized configuration to ensure consistency across all Supabase clients.
 */

/**
 * Storage key for Supabase Auth session in localStorage
 * 
 * IMPORTANT: This key must be consistent across all browser clients
 * to prevent "Multiple GoTrueClient instances" warnings.
 */
export const SUPABASE_AUTH_STORAGE_KEY = "misiones-arrienda-auth";
