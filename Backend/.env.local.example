# FIX-401: Feature flags para chunked cookies + Bearer fallback
# Copiar este archivo a .env.local y configurar según necesidad

# Server-side feature flag
FEATURE_PROFILE_AUTH_FALLBACK=true

# Client-side feature flag
NEXT_PUBLIC_FEATURE_PROFILE_AUTH_FALLBACK=true

# Supabase credentials (ya existentes)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
