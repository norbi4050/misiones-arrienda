# Archivo: Backend/src/app/api/properties/route.ts
# ORM: Supabase (snake_case)
# Descripción: Filtro público con status='PUBLISHED' + is_active=true

diff --git a/Backend/src/app/api/properties/route.ts b/Backend/src/app/api/properties/route.ts
index abc123..def456 100644
--- a/Backend/src/app/api/properties/route.ts
+++ b/Backend/src/app/api/properties/route.ts
@@ -83,8 +83,8 @@ export async function GET(request: NextRequest) {
       // Construir query de Supabase
       let query = supabase
         .from('Property')
-        .select('*', { count: 'exact' })
-      .eq('status', 'PUBLISHED')
-      .eq('is_active', true);
+        .select('*', { count: 'exact' })
+        .eq('status', 'PUBLISHED')
+        .eq('is_active', true);

       // Aplicar filtros avanzados
       if (city) {
